<%= simple_form_for(@ingredient) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :carbs_per_cup %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
